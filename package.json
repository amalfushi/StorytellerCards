{
  "name": "storyteller-cards-monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install && concurrently --names UI,API --prefix-colors cyan,green \"cd UI && npm install\" \"cd API && go mod download\"",
    "install:ui": "cd UI && npm install",
    "install:api": "cd API && go mod download",
    "dev": "concurrently --names UI,API --prefix-colors cyan,green \"cd UI && npm run dev\" \"cd API && go run ./cmd/server\"",
    "dev:ui": "cd UI && npm run dev",
    "dev:ui:localonly": "cd UI && npx vite --mode localonly",
    "dev:api": "cd API && go run ./cmd/server",
    "test": "concurrently --names UI,API \"cd UI && npm test\" \"cd API && go test ./...\"",
    "test:ui": "cd UI && npm test",
    "test:api": "cd API && go test ./...",
    "test:e2e": "cd UI && npx playwright test --config=e2e/playwright.config.ts --project=lifecycle",
    "test:e2e:sync": "cd UI && npx playwright test --config=e2e/playwright.config.ts --project=sync",
    "test:e2e:all": "cd UI && npx playwright test --config=e2e/playwright.config.ts",
    "test:integration": "cd API && go test ./internal/handlers/ -run Roundtrip -v && cd ../UI && npx playwright test --config=e2e/playwright.config.ts"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
